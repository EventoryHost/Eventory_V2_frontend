import React from "react";

const HeroSubtext = () => {
  return (
    <p className="mx-auto mt-6 max-w-[632px] text-center font-figtree font-normal text-[16px] leading-[1.35] tracking-[-0.02em] text-body-secondary">
      Everything your event needs, booked together and handled by one team so
      you can actually be present at your own celebration.
    </p>
  );
};

export default HeroSubtext;
