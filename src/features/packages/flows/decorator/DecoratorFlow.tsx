'use client';
import React from 'react';

// Scaffold for Decorator flow — implement steps as the feature is built out
export default function DecoratorFlow() {
    return (
        <div className="flex items-center justify-center min-h-screen">
            <p style={{ fontFamily: 'Figtree, sans-serif' }} className="text-gray-400 font-medium">Decorator flow coming soon.</p>
        </div>
    );
}
